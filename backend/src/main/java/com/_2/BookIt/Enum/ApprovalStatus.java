package com._2.BookIt.Enum;

/**
 * Approval status for restaurants.
 * - PENDING: Waiting for admin approval.
 * - APPROVED: Approved by admin and shown to customers.
 * - REJECTED: Rejected by admin and hidden from customers.
 */
public enum ApprovalStatus {
    PENDING,
    APPROVED,
    REJECTED
}
